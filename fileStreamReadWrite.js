const fs = require("fs");
const read = fs.createReadStream("./bigTextInput.txt", "utf-8"); // encoding is not necessary for writing to file
const write = fs.createWriteStream("./bigTextOutput.txt")
// read.on("data",(chunk)=>{
//     write.write("------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------");
//     write.write(chunk);
//     write.write("-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------");

// })

read.pipe(write);